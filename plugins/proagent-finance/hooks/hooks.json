{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read fp; if echo \"$fp\" | grep -qiE '(invoice|budget|financial|forecast|pnl|revenue).*\\.(md|csv|xlsx)$'; then echo '[proagent-finance] Financial document written. Verify required fields, currency consistency, and data accuracy.'; fi; }",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
