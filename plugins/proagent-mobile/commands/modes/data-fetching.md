# Mode: data-fetching

**Goal:** Implement data fetching in Expo using fetch, React Query, SWR, or similar patterns.

> **Adequacy Gate**: Before proceeding, verify:
> 1. Project has a data source (API URL, GraphQL endpoint, or local database)
> 2. User has chosen a fetching strategy (fetch, React Query, or SWR)
> If any check fails, inform the user and collect missing prerequisites before continuing.

**Announce:** "Starting data fetching workflow. I'll help you set up the right data fetching strategy for your app."

> **CONTEXT GUARD**: ONLY read the reference file below now that the user has selected this mode.

Read and execute the guidance in `skills/mobile-assistant/references/native-data-fetching.md`.
